import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    marginBottom: 20,
  },
  inputsContainer: {
    flex: 1,
    flexDirection: 'row',
    marginBottom: 5,
  },
  firstInput: {
    marginRight: 10,
  },
});
